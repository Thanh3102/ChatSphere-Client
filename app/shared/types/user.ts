export type UserBasicInfo = {
  id: string;
  name: string;
  email: string;
  image: string;
};

export type UserDetailInfo = {
  id: string;
  name: string;
  email: string;
  image: string;
  dateOfBirth: Date;
  phoneNumber: string;
  gender: string;
};
